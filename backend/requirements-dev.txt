# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Database (SQLite for local dev — no PostgreSQL needed)
sqlalchemy>=2.0.36
aiosqlite>=0.20.0

# Scraping
google-play-scraper==1.2.4
app-store-scraper==0.3.5
praw==7.7.1

# Embeddings + LLM
openai>=1.50.0

# Data processing — pydantic v2 latest has Python 3.14 wheels
pydantic>=2.10.0
pydantic-settings>=2.6.0
python-dotenv==1.0.1
beautifulsoup4>=4.12.3
# lxml removed — uses html.parser (stdlib) instead; no C++ build tools needed

# Utilities
httpx>=0.27.0
tenacity>=8.3.0
